# AI桌面小车机器人产品设计文档（V1可落地方案）

---

# 一、产品目标（Product Vision）

## 🎯 核心目标

打造一个**低成本、即插即用的具身AI小车机器人**，用户只需：

* 配置 WiFi（或手机热点）
* 输入云端 LLM API Key

即可实现：

* 🎤 语音交互（对话 + 控制）
* 👀 基础视觉能力（后续扩展）
* 🚗 小车运动控制
* 🤖 简单具身任务执行

---

## 🧠 产品定位

> 一个“云驱动的具身AI终端设备”

类似：

* 智能音箱（语音入口）
* * 机器人身体（移动/执行）

---

## ⚠️ 核心设计原则

1. **低延迟优先（比智能更重要）**
2. **云端为主，本地为辅**
3. **快慢路径分离（关键架构）**
4. **用户无需复杂配置**

---

# 二、系统整体架构

## 🏗️ 架构图

```text
[iPhone App]
  ├── 语音识别（本地）
  ├── UI交互
  ├── 快速指令解析（本地）
  └── 云AI调用

        ↓ WiFi（热点/局域网）

[ESP32-S3]
  ├── 通信桥接
  ├── 指令解析
  └── 状态反馈

        ↓ 串口/I2C

[CyberBrick]
  └── 电机 / 舵机控制（OpenClaw）
```

---

## 🧩 模块职责划分

| 模块         | 职责                 |
| ---------- | ------------------ |
| iPhone     | 语音 + AI + UI       |
| 云端         | LLM / ASR / Vision |
| ESP32      | 通信 & 控制转发          |
| CyberBrick | 执行控制               |

---

# 三、信息流设计（核心）

## 🔥 关键理念：快路径 + 慢路径

---

## 🟢 快路径（Fast Path）

👉 目标：**< 500ms 内响应**

```text
语音 → 本地识别 → 关键词匹配 → 直接控制小车
```

### 示例：

用户说：

> “前进”

流程：

```text
iPhone识别 → 匹配“前进” → 发送 forward → ESP32 → 执行
```

⏱️ 延迟：200~500ms

---

## 🔴 慢路径（Slow Path）

👉 目标：处理复杂AI任务

```text
语音 → 云ASR → LLM → 决策 → 控制
```

⏱️ 延迟：1~3秒

---

## ⚡ 合并策略（关键）

```text
用户说话
  ↓
立即反馈（灯/声音）
  ↓
快路径先执行（基础动作）
  ↓
慢路径补充/修正
```

---

# 四、延迟预算（必须控制）

| 阶段    | 目标时间    |
| ----- | ------- |
| 初始反馈  | < 100ms |
| 快路径执行 | < 500ms |
| AI响应  | < 2s    |
| 总体体验  | < 1s体感  |

---

# 五、ESP32端设计

## 📡 功能

* 接收HTTP/WebSocket指令
* 转发给CyberBrick
* 状态回传

---

## 🧾 指令协议（JSON）

```json
{
  "action": "move",
  "direction": "forward",
  "duration": 2
}
```

---

## 📌 支持指令（V1）

```json
{
  "fast_commands": [
    "forward",
    "backward",
    "left",
    "right",
    "stop"
  ]
}
```

---

## 🔧 建议实现

* HTTP Server（简单）
* 或 WebSocket（低延迟）

---

# 六、iPhone App设计（重点🔥）

## 🎯 核心功能模块

---

## 1️⃣ 语音模块

### 功能：

* 实时语音识别（Speech Framework）
* 流式识别

### 输出：

```json
{
  "text": "向前走"
}
```

---

## 2️⃣ 快速指令识别（本地）

### 逻辑：

```pseudo
if text 包含 "前进":
    send("forward")
elif text 包含 "停止":
    send("stop")
```

---

## 3️⃣ 云AI模块

### 调用：

* Whisper（可选）
* LLM（OpenAI API）

---

### 输入：

```json
{
  "user_input": "帮我把那个东西拿过来"
}
```

---

### 输出：

```json
{
  "action": "pick_object",
  "target": "object"
}
```

---

## 4️⃣ 控制通信模块

* HTTP / WebSocket
* 自动重连
* mDNS支持（robot.local）

---

## 5️⃣ UI设计（重要）

### 必须有：

* 🎤 语音按钮
* 🟢 状态指示（听/思考/执行）
* 📝 文本显示（识别结果）
* 🎮 手动控制（备用）

---

## 6️⃣ 状态反馈设计（体验关键）

| 状态 | 表现      |
| -- | ------- |
| 听  | 呼吸灯     |
| 思考 | 闪烁      |
| 执行 | 动作 + 声音 |

---

# 七、用户配网流程（产品化关键）

## 📶 配网模式

1. 设备开启热点：

   ```
   Robot_XXXX
   ```
2. 用户连接
3. 自动弹出网页
4. 输入：

   * WiFi
   * 密码
   * API Key

---

## 📄 配网页面字段

```json
{
  "wifi_ssid": "",
  "wifi_password": "",
  "api_key": ""
}
```

---

# 八、成本 vs 体验策略

## 🟢 V1推荐方案

* ESP32 + CyberBrick
* iPhone做语音
* 云端LLM

👉 成本低 + 体验好

---

## 🟡 V2优化

* 加本地唤醒词
* 流式语音
* 视觉模块

---

# 九、可参考开源项目（强烈建议复用）

## 🔧 AI交互参考

* StackChan（语音 + 表情 + AI流程）

---

## 🔧 ESP32通信

* ESPAsyncWebServer
* Arduino WebSocket

---

## 🔧 iOS开发

* Speech Framework（语音识别）
* URLSession / WebSocket

---

## 🔧 云AI

* OpenAI API
* Whisper API

---

# 十、开发优先级（必须按顺序）

## Phase 1（打通链路）

* iPhone → ESP32 控制小车
* HTTP控制

---

## Phase 2（语音控制）

* 语音识别 → 控制动作

---

## Phase 3（AI接入）

* LLM → 指令解析

---

## Phase 4（优化体验）

* 流式
* 快慢路径
* 状态反馈

---

# 十一、成功标准（验收指标）

## ✅ 技术指标

* <500ms 动作响应
* <2s AI响应
* 稳定连接

---

## ✅ 用户体验

* 说话后立即有反馈
* 小车快速响应
* AI对话自然

---

# 十二、核心总结（给Claude Code）

> 该系统必须实现：
>
> 1. 快慢路径分离
> 2. 本地优先执行简单指令
> 3. 云端处理复杂AI
> 4. 全程有状态反馈
> 5. 用户感知延迟 < 1秒

---

# 🚀 最终目标

构建一个：

👉 **“即插即用、低延迟、可扩展的具身AI机器人平台”**
